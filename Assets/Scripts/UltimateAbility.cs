using UnityEngine;
using System.Collections; // Add this line

public class RocketLaunchButtonController : MonoBehaviour
{
    /*
    public PlayerRocketLauncher playerRocketLauncher; // Reference to the PlayerRocketLauncher script.
    public float disableTime = 1f; // Time for which the button will be disabled after launching a rocket.

    private void Start()
    {
        // Assuming the button is initially enabled.
        playerRocketLauncher.SetRocketLaunchButtonEnabled(true);
    }

    public void LaunchRocket()
    {
        // Disable the rocket launch button.
        playerRocketLauncher.SetRocketLaunchButtonEnabled(false);

        // Launch the rocket.
        playerRocketLauncher.LaunchRocket();

        // Wait for the disableTime before re-enabling the button.
        StartCoroutine(ReenableRocketLaunchButtonAfterDelay(disableTime));
    }

    private IEnumerator ReenableRocketLaunchButtonAfterDelay(float delay)
    {
        yield return new WaitForSeconds(delay);
        // Re-enable the rocket launch button.
        playerRocketLauncher.SetRocketLaunchButtonEnabled(true);
    }
    */
}
